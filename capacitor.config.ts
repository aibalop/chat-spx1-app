import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'chat-spx1-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
